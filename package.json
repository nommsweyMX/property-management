{
  "name": "casa-property-management",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check public/app.mjs && node --check server.mjs && node --check server/airtable.mjs"
  }
}
